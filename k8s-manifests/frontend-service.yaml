apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  labels:
    app: todo-chatbot
    component: frontend
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
      nodePort: 30000
  selector:
    app: todo-chatbot
    component: frontend
