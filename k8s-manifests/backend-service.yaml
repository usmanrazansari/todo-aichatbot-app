apiVersion: v1
kind: Service
metadata:
  name: todo-backend
  labels:
    app: todo-chatbot
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      name: http
  selector:
    app: todo-chatbot
    component: backend
